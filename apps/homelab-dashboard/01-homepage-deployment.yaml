# 1. Create a 1GB persistent volume for your dashboard configurations
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-config-pvc
  namespace: homelab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
---
# 2. Update the Deployment to mount the volume and whitelist the domain
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homepage
  namespace: homelab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homepage
  template:
    metadata:
      labels:
        app: homepage
    spec:
      containers:
      - name: homepage
        image: ghcr.io/gethomepage/homepage:latest
        ports:
        - containerPort: 3000
        env:
        # Whitelist the domains so Next.js accepts the traffic from NGINX
        - name: HOMEPAGE_ALLOWED_HOSTS
          value: "home.cloujen.com,start.cloujen.com"
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
      volumes:
      - name: config-volume
        persistentVolumeClaim:
          claimName: homepage-config-pvc
---
# 3. The Service
apiVersion: v1
kind: Service
metadata:
  name: homepage-svc
  namespace: homelab
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: homepage